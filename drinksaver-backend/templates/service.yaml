apiVersion: v1
kind: Service
metadata:
  name: {{ include "drinksaver-backend.fullname" . }}
  labels:
    {{- include "drinksaver-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drinksaver-backend.selectorLabels" . | nindent 4 }}
